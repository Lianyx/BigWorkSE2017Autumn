<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.inventorydata.InventoryDamageBiiPOMapper">
    <resultMap id="resultMap" type="po.InventoryDamageBillPO">
        <result typeHandler="MyJsonHandler" column="goodsList" property="goodsList" />
    </resultMap>

    <select id="getDayId" resultType="int">
        select max(dayId)+1 from inventorydamagebill where createTime &gt;= #{today}
    </select>
    <insert id="insert">
        insert into inventorydamagebill(dayId, operatorId, createTime, lastModifiedTime, receiptState, clerkName,  goodsList, comment)
        values(#{dayId}, #{operatorId}, #{createTime}, #{lastModifiedTime}, #{receiptState}, #{clerkName}, #{goodsList, typeHandler=MyJsonHandler},  #{comment})
    </insert>
    <update id="update">
        update inventorydamagebill set lastModifiedTime=#{lastModifiedTime}, receiptState=#{receiptState}, clerkName=#{clerkName}, goodsList=#{goodsList, typeHandler=MyJsonHandler},  comment=#{comment}
        where dayId=#{dayId} and createTime=#{createTime}
    </update>
    <delete id="delete">
        delete from inventorydamagebill where dayId=#{dayId} and createTime=#{createTime}
    </delete>
    <select id="selectBetween" resultMap="resultMap">
        select * from inventorydamagebill where createTime &gt;= #{begin} and createTime &lt;= #{end}
    </select>
    <select id="selectByState" resultMap="resultMap">
        select * from inventorydamagebill where receiptState=#{receiptState}
    </select>

</mapper>